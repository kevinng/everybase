"""Base processor functions - i.e., functions operating on messages at the
frontline basis. These are the functions that will operate directly on
messages. We also have functions that operates on outputs on base functions,
thus allowing us to define processing 'flows' and logic. Those functions are
defined in a separate file.
"""